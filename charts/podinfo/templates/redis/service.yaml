{{- if .Values.redis.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "userlocationapi.fullname" . }}-redis
  labels:
    app: {{ template "userlocationapi.fullname" . }}-redis
spec:
  type: ClusterIP
  selector:
    app: {{ template "userlocationapi.fullname" . }}-redis
  ports:
    - name: redis
      port: 6379
      protocol: TCP
      targetPort: redis
{{- end }}
